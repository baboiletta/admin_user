<%=link_to "Back to List", subjects_path %>
<div>
    <h1> Subject Edit</h1>
    <%= form_for(@subject, url: @subject, method: 'patch') do |f|%>
        <%= render partial: "form", locals: {f: f}%>
        <div>   
            <%=f.submit("Update Subject")%>
        </div>
        <%end%>
</div>

